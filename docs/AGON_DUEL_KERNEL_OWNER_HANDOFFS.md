# Duel Kernel Owner Handoffs

`Agents-of-Abyss` owns the duel kernel law and registry.

`aoa-sdk` owns typed helper candidates. `abyss-stack` owns local runtime substrate shape. Evals, memo, and stats remain future owners of checks, candidate memory intake, and derived observability.
