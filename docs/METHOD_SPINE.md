# Method Spine

This document records the current second-wave doctrine for AoA after the trunk-first structural wave.

It is not a replacement for the rootline.
It is the next planting law that follows from the rootline once the structural seams are explicit.

## Core doctrine

The current second-wave method spine is:

1. **method lives in playbooks**
2. **donor refinement stays source-first**
3. **maturation must be evidence-backed**

## Method lives in playbooks

AoA keeps these distinctions:

- `technique` owns reusable practice
- `skill` owns bounded execution
- `playbook` owns recurring scenario-level method

When a route spans skills, roles, memory posture, fallback posture, and proof hooks, it should not stay smeared across neighboring layers.
It should become a readable playbook-owned route.

## Source-first donor refinement

AoA may learn from external donors, but it should do so through an explicit refinery:

`donor -> repeated operational pattern -> sanitized technique or skill -> bounded playbook -> eval proof surface`

That refinery is strict about what gets extracted:

- extract reusable pattern
- keep provenance readable
- reject contamination that widens the contract carelessly
- reject foreign doctrine when it tries to arrive as canon instead of as bounded evidence or governance pattern

## Evidence-backed maturity

AoA may use one shared maturity ladder when speaking across repositories:

- `seed`
- `proven`
- `promoted`
- `canonical`
- `deprecated`

This ladder is for ecosystem-level legibility.

It does not force every repository to rename its local statuses.
Instead, it gives the federation one explicit crosswalk for public claims about maturity.

## Crosswalk rule

Use the shared ladder when:

- the center summarizes cross-repo maturity
- one repo compares its public posture to another repo's public posture
- a route depends on evidence-backed movement between repositories

Keep repository-local ladders when:

- a repo needs finer-grained staging inside its own contract
- local validator or review doctrine already names narrower stages

## Not this wave

This doctrine does not open:

- witness / compost protocol implementation
- counterpart-edge mapping in `aoa-kag`
- graph-first bridge expansion
- score systems with no evidence posture

The current job is to give method a real home, keep donor intake legible, and make maturity claims honest.
