# Replayable Decision History

Every meaningful governance runtime event carries a prev_hash and event_hash. Replay verifies order, integrity, and authority references.

If the chain breaks, downstream enforcement pauses until audit resolves the gap.
