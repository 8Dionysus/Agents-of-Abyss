# Experience v2.0 Living Workspace Continuity Runtime

This surface lands the `v2.0` living-workspace continuity runtime center
contract after the `v1.9` continuity loom center contract.

It is Wave 10 of the current `v1.2 -> v2.0` planting campaign. It is not
`Experience Wave X`, because existing `AGON_WAVE10_LANDING` surfaces already
own different contradiction / closure / summon law meaning.

It is not a live workspace runtime, not a hidden `.codex/continuity`
installation, not a background resume loop, not a worker or daemon surface,
not direct `Tree-of-Sophia` runtime write, and not a claim that the workspace
has become a sovereign continuity actor.

## Source Frame

The source transport artifact is:

- `aoa-experience-living-workspace-continuity-runtime-seed-v2_0.zip`

The `Dionysus` intake records its SHA-256 as:

- `2247d4095c52412512063bcac1b07b6db83539ac4597bd9b67a82adc3ba26d71`

Archive-local dry-run evidence was rechecked before this landing:

- `SCHEMA_EXAMPLE_MAP.json` contains `55` schema/example pairs
- the archive contains `163` JSON files
- the archive inventory names `317` files total
- the seed test suite exposes `18` passing tests
- the seed names `.codex/continuity` as its proposed runtime seam

That evidence proves archive readability and local coherence only. It does not
grant workspace-runtime authority, shared-root install authority, memo
sovereignty, routing sovereignty, assistant self-continuity authority, or
direct `Tree-of-Sophia` write.

## Predecessor Law

This surface depends on and remains subordinate to:

- `EXPERIENCE_V1_2_TO_V2_0_BRIDGE.md`
- `EXPERIENCE_V1_8_CONTEXT_ROUTING_NERVOUS_SYSTEM.md`
- `EXPERIENCE_V1_9_CONTEXT_MEMORY_WEAVING_CONTINUITY_LOOM.md`
- `SELF_AGENCY_CONTINUITY.md`
- `FEDERATION_RULES.md`
- `EXPERIENCE_RUNTIME_AUTHORITY_BOUNDARY.md`
- `EXPERIENCE_REPO_LANDING_ORDER.md`
- `EXPERIENCE_TOS_CANDIDATE_BOUNDARY.md`
- `8Dionysus:docs/WORKSPACE_INSTALL.md`
- `8Dionysus:docs/CODEX_PLANE_REGENERATION.md`
- `AGON_WAVE10_LANDING.md`

This landing adapts the `v2.0` seed into those current stop-lines instead of
replaying the archive over them.

## Core Law

The `v2.0` center contract is a bounded, non-runtime grammar for a future
living-workspace seam:

```text
source seed
  -> predecessor check
  -> runtime charter request
  -> shared-root projection boundary
  -> workspace helper seam review
  -> runtime substrate gate
  -> continuity memory gate
  -> assistant run posture review
  -> receipt-backed live reentry candidate
  -> replay and runtime integrity check
  -> canonical runtime boundary review
  -> owner landing or separate runtime-owner gate request
```

Its laws are:

- No live workspace runtime activation.
- No hidden `.codex/continuity` installation.
- No self-continuity sovereignty.
- No assistant self-authorized continuity.
- No Codex durable continuity approval.
- No checkpoint export without budget and evidence.
- No memory owner theft.
- No personal-context overreach.
- No stale context as fresh evidence.
- No replay hash-break bypass.
- No route-loop runtime escalation.
- No runtime migration without backup and operator gate.
- No worker, scheduler, or daemon activation.
- No direct `Tree-of-Sophia` runtime write.
- No dashboard or state-machine sovereignty.
- No continuity-as-consciousness claim.
- No raw archive or generated runtime promotion into owner truth.

## Workspace Boundary

`living workspace continuity runtime` here is only a center boundary for later
owner-local work. It may name the proposed seam, quarantine runtime object
classes, and require explicit owner gates before projection, install, storage,
resume, or replay surfaces land elsewhere.

`8Dionysus` may govern shared-root projection law because it owns the
source-owned copies for selected workspace-root surfaces. That does not make
`8Dionysus` the owner of runtime continuity truth.

`aoa-sdk` may later own typed helper seams. That does not make `aoa-sdk` the
owner of workspace authority.

`abyss-stack` may later own actual runtime substrate. That does not make the
center a runtime operator today.

## Runtime Flow

The center flow is:

```text
source_seed_received
  -> wave9_continuity_predecessor_checked
  -> living_workspace_runtime_charter_requested
  -> shared_root_projection_boundary_requested
  -> workspace_helper_seam_requested
  -> runtime_substrate_gate_requested
  -> continuity_memory_gate_requested
  -> continuity_run_posture_checked
  -> live_session_reentry_route_requested
  -> runtime_integrity_and_replay_checked
  -> canonical_runtime_boundary_checked
  -> derived_runtime_observability_candidate_requested
  -> owner_landing_or_runtime_gate_request_declared
```

The flow names a future runtime law. It does not install `.codex/continuity`,
does not start a background service, does not create a live assistant
continuity run, and does not project shared-root surfaces into runtime truth.

## Hard Guards

The non-negotiable `v2.0` guards are:

- `no_live_workspace_runtime_activation`
- `no_hidden_codex_continuity_installation`
- `no_self_continuity_sovereignty`
- `no_assistant_self_authorized_continuity`
- `no_codex_durable_continuity_approval`
- `no_checkpoint_export_without_budget_and_evidence`
- `no_memory_owner_theft`
- `no_personal_context_overreach`
- `no_stale_context_as_fresh_evidence`
- `no_replay_hash_break_bypass`
- `no_route_loop_runtime_escalation`
- `no_runtime_migration_without_backup_and_operator_gate`
- `no_worker_scheduler_or_daemon_activation`
- `no_direct_tree_of_sophia_runtime_write`
- `no_dashboard_or_state_machine_sovereignty`
- `no_continuity_as_consciousness_claim`
- `no_raw_archive_or_generated_runtime_promotion`

## Blocking Contracts

The `v2.0` center contract treats archive-local layout manifests, feeds,
thread indexes, checkpoint exports, operator console state, runtime events,
replay audits, dashboards, state machines, migrations, rollbacks, bootstrap
flows, smoke results, live run receipts, and all owner-local fanout under
`aoa-memo`, `aoa-routing`, `aoa-agents`, `aoa-evals`, `aoa-stats`,
`aoa-playbooks`, `aoa-sdk`, `aoa-kag`, `abyss-stack`, `aoa-skills`,
`aoa-techniques`, and `Tree-of-Sophia` as quarantine evidence unless an owner
repository later lands an adapted surface through its own gate.

The center contract requires:

- landing states remain draft, proposed, reviewed, blocked, quarantined, or
  candidate-only; `active` is not a landing state
- no shared-root projection may bypass the source-owned `8Dionysus` copies
- no `.codex/continuity` path may be installed, projected, or activated here
- no assistant, Codex, memo, routing, stats, SDK, or dashboard surface may
  steal workspace authority
- checkpoint export requires budget and evidence
- stale, replay-broken, or looped continuity must stay blocked or quarantined
- runtime migration requires backup and explicit operator gating
- workers, schedulers, daemons, storage writeback, and auto-resume remain
  disabled

## Owner Split

- `Agents-of-Abyss` owns this `v2.0` center contract, predecessor mapping,
  hard guards, quarantine list, and human gates.
- `8Dionysus` owns shared-root projection law and the source-owned copies of
  `AGENTS.md`, `AOA_WORKSPACE_ROOT`, `.agents/`, and `.codex/` only.
- `aoa-sdk` owns typed workspace discovery, ingress / guard, and helper seams
  only.
- `aoa-memo` owns continuity and reentry candidates only, never workspace
  sovereignty.
- `aoa-routing` owns receipt-backed route hints only.
- `aoa-agents` owns continuity-backed assistant posture only.
- `aoa-evals` owns replay, integrity, and runtime legitimacy proof only.
- `aoa-stats` owns derived summaries only.
- `aoa-playbooks` owns recurring choreography only if that route later matures.
- `aoa-kag` owns derived pattern lift only after later owner-local landing.
- `abyss-stack` owns actual runtime, export, cache, worker, scheduler, and
  daemon surfaces only after a separate runtime-owner gate.
- `Tree-of-Sophia` owns dossier and canon intake only through its own review
  path.
- `Dionysus` owns source lineage and later harvest trace only.

## Stop-Lines

This landing must not:

- collapse `v2.0` into raw archive replay or generated clean-flow promotion
- rename itself into `Experience Wave X`
- install or project `.codex/continuity`
- treat projected shared-root surfaces as self-owning runtime truth
- let memo, routing, stats, SDK, or dashboards claim workspace sovereignty
- create live checkpoint export, live assistant continuity run, migration,
  rollback, cache, worker, scheduler, daemon, or background resume loop
- write directly into `Tree-of-Sophia`

## Result

If this contract is planted correctly, the ecosystem gains a lawful and
reviewable center grammar for a future living-workspace continuity seam without
turning that seam on.

What lands here is the final center law before later owner-local hardening.
What does not land here is live runtime continuity.
