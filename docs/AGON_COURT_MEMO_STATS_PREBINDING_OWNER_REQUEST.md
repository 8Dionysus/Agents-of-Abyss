# Agon Wave VII Owner Request

The owner request is simple:

- `aoa-evals`: prepare checks, not verdict rule.
- `aoa-memo`: prepare candidate intake, not durable scar writes.
- `aoa-stats`: prepare derived summaries, not authority.
- `aoa-sdk`: prepare recurrence review lanes, not hidden scheduling.

Wave VII must be reviewable, source-owned, and pre-protocol.
