# Agon Mechanical Trial Owner Handoffs

`Agents-of-Abyss` owns the center trial-suite law.

`aoa-playbooks` owns trial choreography bindings.
`abyss-stack` owns local dry-run event-log substrate.
`aoa-evals` owns candidate checks.
`aoa-memo` owns candidate intake surfaces.
`aoa-stats` owns derived observability candidates.
`aoa-sdk` owns typed helper candidates.

No owner receives final arena sovereignty in this wave.
