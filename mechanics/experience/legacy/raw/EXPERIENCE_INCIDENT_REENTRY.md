# Incident Re-entry

Deployment incidents are not dead-end logs. A material incident becomes a new experience candidate route.

```text
deployment incident
  -> alarm verdict
  -> memory gate
  -> owner landing
  -> patch proposal or rollback note
  -> regression forge
```

This prevents post-release reality from being quarantined outside the learning loop.
