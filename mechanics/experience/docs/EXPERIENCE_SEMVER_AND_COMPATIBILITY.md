# Experience SemVer and Compatibility


Assistant releases use service semver:

- PATCH: stricter receipt, clearer escalation, safer output formatting.
- MINOR: new allowed behavior inside existing mandate.
- MAJOR: mandate, identity, persistent policy or service contract changes.

Major changes require re-chartering. A patch born from experience cannot disguise itself as a tiny update if it changes the service promise.
