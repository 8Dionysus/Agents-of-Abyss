# Non-Identity Guard Validation

Use the package validator named in `../../AGENTS.md`:
`validate_boundary_bridge_distillation.py`.

Then do manual phrase review.

Search for wording that turns `support`, `analogy`, `projection`, or
`compatibility` into identity.
