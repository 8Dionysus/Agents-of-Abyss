# Compatibility Route Validation

Use the package validator named in `../../AGENTS.md`:
`validate_boundary_bridge_distillation.py`.

Manual review: confirm adapters, generated surfaces, and public projections do
not author meaning.
