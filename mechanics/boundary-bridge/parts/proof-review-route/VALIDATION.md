# Proof Review Route Validation

Use the package validator named in `../../AGENTS.md`:
`validate_boundary_bridge_distillation.py`.

Manual review: confirm evidence exists before any public or release-facing
claim.
