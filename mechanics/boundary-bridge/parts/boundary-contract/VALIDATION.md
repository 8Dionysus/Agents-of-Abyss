# Boundary Contract Validation

Use the package validator named in `../../AGENTS.md`:
`validate_boundary_bridge_distillation.py`.

Manual review: confirm both owners and the no-transfer rule are explicit.
