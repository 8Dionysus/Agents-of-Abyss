# Derived Projection Validation

Use the package validator named in `../../AGENTS.md`:
`validate_boundary_bridge_distillation.py`.

Manual review: confirm the derived surface points back to stronger source
truth.
