# ToS Support Validation

Use the package validator named in `../../AGENTS.md`:
`validate_boundary_bridge_distillation.py`.

Manual review: confirm the bridge does not restate ToS doctrine as AoA-owned
meaning.
