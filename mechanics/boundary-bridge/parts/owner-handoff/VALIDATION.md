# Owner Handoff Validation

Use the package and owner-request validators named in `../../AGENTS.md`:
`validate_boundary_bridge_distillation.py`, `validate_owner_request_queue.py`,
and `validate_owner_request_docs.py`.

Manual review: confirm `requested` status is not described as accepted.
