# Counterpart Edge Validation

Use the package validator named in `../../AGENTS.md`:
`validate_boundary_bridge_distillation.py`.

Manual review: reject edges that lack mode, source refs, or non-identity note.
