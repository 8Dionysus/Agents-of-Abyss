# ToS Bridge Owner-repo Requests

This file is a compatibility route for the old ToS-specific request surface.

Canonical boundary-bridge owner requests now live in
[OWNER_REQUESTS](../OWNER_REQUESTS.md). The current ToS support requests are
listed there with `ORQ-BRIDGE-*` identifiers.

## Historical note

This document used to own the `tos-bridge` request list. It no longer owns
canonical request truth. Keep it only as a route for older links and historical
readers.

## Validation

Use the validation lane in [AGENTS.md](AGENTS.md#validation).
