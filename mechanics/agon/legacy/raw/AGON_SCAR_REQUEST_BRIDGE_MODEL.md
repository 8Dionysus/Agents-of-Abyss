# Agon Scar Request Bridge Model

A scar request is a bridge artifact addressed to `aoa-memo`. It is not a durable scar.

The future arena may discover that an actor should remember a rupture. Wave XI only defines how that request should be shaped before memory ownership receives it.

The request must explicitly set:

```text
memory_owner: aoa-memo
durable_write_allowed: false
```
