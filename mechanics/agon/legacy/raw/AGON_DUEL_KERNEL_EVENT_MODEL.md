# Duel Kernel Event Model

The event model is monotonic and hash-chain traceable. It reserves space for commits, reveal, engagement, revision, adjudication request, and closeout candidate.

`adjudication_request` is not a verdict. `closeout_candidate` is not closure.
