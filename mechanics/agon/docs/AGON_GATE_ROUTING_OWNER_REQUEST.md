# Agon Gate Routing Owner Request

This request asks `aoa-routing` to grow one additive pre-protocol surface:

`generated/agon_gate_routing_registry.min.json`

The surface should describe trigger classes, route hints, source authority references, and stop-lines for recognizing possible Agon entry.

## Required source posture

The routing surface should read or reference these owners without copying their meaning:

- `Agents-of-Abyss` for Agon law and lawful moves
- `aoa-agents` for actor kind, office, and formation readiness
- `aoa-evals` for proof and breach checks
- `aoa-memo` for future scars, delta history, and retention memory
- `aoa-playbooks` for trial ritual choreography
- `aoa-stats` for derived observability
- `Tree-of-Sophia` for slow canon boundary only

## Request status

This is a pre-protocol request.

It prepares future arena entry.
It does not activate arena entry.
