# Agon Duel Kernel Model

Wave XII introduces the first bounded mechanical duel kernel model.

The kernel is not a full arena service. It binds earlier Agon surfaces into a minimal duel contour: two agonic contestants, witnesses, a judge candidate, and an assistant steward. It may be represented as a local event log through `abyss-stack`, but it cannot issue live verdicts or write durable scars.
