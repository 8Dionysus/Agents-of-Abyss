# Fragility Blacklist Route

This file is a compatibility route for the former root fragility blacklist.

The source blacklist now lives at
[mechanics/antifragility/FRAGILITY_BLACKLIST.md](mechanics/antifragility/FRAGILITY_BLACKLIST.md).

## Current Role

Root keeps this short route because fragility patterns are frequently referenced
from public entry and release review surfaces.
The antifragility package owns the actual list, its pruning posture, and its
mechanic-local stop-lines.

## Rule

Update the source file under `mechanics/antifragility/` first.
Do not expand this root route into a second blacklist.
